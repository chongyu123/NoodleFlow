<%= form_for([@pad,@attachment], :html => {:multipart => true}) do |f| %>
  <% if @attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% @attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <label for="new_flie">File</label><br />
  <%= file_field_tag 'new_file' %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description, :cols => 80 %>
  </div>

  <div class="actions">
    <%= f.submit "Upload" %>
  </div>

<% end %>

